- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.gitconfig:
    ~/.ssh:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
